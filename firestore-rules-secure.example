rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // REGRAS MAIS SEGURAS PARA PRODUÇÃO
    // Descomente e use estas regras quando implementar autenticação adequada
    
    /*
    // Regras baseadas em autenticação
    match /insumos/{document} {
      allow read, write: if request.auth != null;
    }
    
    match /compras/{document} {
      allow read, write: if request.auth != null;
    }
    
    match /fichasTecnicas/{document} {
      allow read, write: if request.auth != null;
    }
    
    match /pratos/{document} {
      allow read, write: if request.auth != null;
    }
    
    match /configuracoes/{document} {
      allow read, write: if request.auth != null;
    }
    */
    
    // REGRAS ATUAIS - DESENVOLVIMENTO/DEMONSTRAÇÃO
    // Permitir acesso completo para demonstração
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
